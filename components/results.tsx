import Image from "next/image"
import { Calendar, MapPin, Trophy } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import matches from "@/data/matches.json"

export function Results() {
  const latest = [...matches].filter((match) => match.status === "completed").sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())[0]
  if (!latest) return null
  const date = new Date(latest.date.replace(" ", "T"))

  return <section className="bg-gradient-to-r from-primary to-accent py-16"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="text-center"><Badge className="mb-4 bg-background/20 text-primary-foreground hover:bg-background/30">Último Resultado</Badge></div><div className="mt-8 flex flex-col items-center justify-center gap-8 md:flex-row md:gap-16"><div className="flex flex-col items-center gap-4"><div className="flex h-28 w-28 items-center justify-center rounded-full bg-background/10 p-3 backdrop-blur">{latest.team_a_logo ? <Image src={latest.team_a_logo} alt={`Escudo de ${latest.team_a}`} width={100} height={100} className="h-full w-full object-contain" /> : <Trophy className="h-14 w-14 text-primary-foreground" aria-label={`Copa para ${latest.team_a}`} />}</div><h3 className="font-[family-name:var(--font-bebas)] text-xl text-primary-foreground sm:text-2xl">{latest.team_a}</h3></div><div className="flex flex-col items-center gap-2"><div className="flex items-center gap-4"><span className="font-[family-name:var(--font-bebas)] text-6xl text-primary-foreground sm:text-8xl">{latest.score_team_a}</span><span className="font-[family-name:var(--font-bebas)] text-4xl text-primary-foreground/50">-</span><span className="font-[family-name:var(--font-bebas)] text-6xl text-primary-foreground sm:text-8xl">{latest.score_team_b}</span></div><div className="flex flex-wrap justify-center gap-4 text-sm text-primary-foreground/70"><span className="flex items-center gap-1"><Calendar className="h-4 w-4" />{date.toLocaleDateString("es-ES")}</span><span className="flex items-center gap-1"><MapPin className="h-4 w-4" />{latest.location}</span></div></div><div className="flex flex-col items-center gap-4"><div className="flex h-28 w-28 items-center justify-center rounded-full bg-background/10 p-3 backdrop-blur">{latest.team_b_logo ? <Image src={latest.team_b_logo} alt={`Escudo de ${latest.team_b}`} width={100} height={100} className="h-full w-full object-contain" /> : <Trophy className="h-14 w-14 text-primary-foreground" aria-label={`Copa para ${latest.team_b}`} />}</div><h3 className="font-[family-name:var(--font-bebas)] text-xl text-primary-foreground sm:text-2xl">{latest.team_b}</h3></div></div></div></section>
}
